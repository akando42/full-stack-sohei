/*!
 * isNonNullObject v1.0.0
 * https://github.com/fengyuanchen/is-non-null-object
 *
 * Copyright 2018-present Chen Fengyuan
 * Released under the MIT license
 *
 * Date: 2018-05-23T12:40:43.649Z
 */

(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory() :
  typeof define === 'function' && define.amd ? define(factory) :
  (global.isNonNullObject = factory());
}(this, (function () { 'use strict';

  /**
   * Check if the given value is a non-null object.
   * @param {*} value - The value to check.
   * @returns {boolean} Returns `true` if the given value is a non-null object, else `false`.
   */
  function isNonNullObject(value) {
    return typeof value === 'object' && value !== null;
  }

  return isNonNullObject;

})));
